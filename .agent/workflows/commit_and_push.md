---
description: Commit and push changes to the repository
---
1. Check the status of the repository using `git status`.
2. Generate a conventional commit message based on the changes.
3. Show the proposed commit message to the user and ask for confirmation or edits.
4. Once approved, run the following commands:
   ```bash
   git add .
   git commit -m "<message>"
   git push
   ```
